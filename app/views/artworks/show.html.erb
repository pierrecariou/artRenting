<div class="wrapper">
<center>
  <h1> <%= @artwork.name %> </h1>
  <%= cl_image_tag @artwork.photo, height: 500, width: 600, crop: :fill %>
</center>
<center>
  <%= @artwork.category %>
  <br />
  <%= @artwork.availability %>
  <% if policy(@artwork).edit? %>
    <%= link_to  edit_artwork_path(@artwork) do %>
    <i class="fas fa-edit"></i>
  <% end %>
  </center>
  <% end %>


</div>
